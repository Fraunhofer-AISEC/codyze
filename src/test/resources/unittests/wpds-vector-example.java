import java.util.Vector;

/* Code ist below at line 245 to align with example from paper */




















































































































































































































































public class WpdsVectorExample {
  void foo() {
    Vector v = new Vector();
    v.add();
    v.lastElement();
    v.clear();
    accessLastElement(v);  // NOK: getting last element from now empty Vector
  }

  Object accessLastElement(Vector u) {
    Object o = u.lastElement();
    return o;
  }
}